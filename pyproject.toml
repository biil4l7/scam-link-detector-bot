[project]
name = "scam-link-detector-bot"
version = "1.0.0"
description = "Telegram bot that checks if links are safe or dangerous"
requires-python = ">=3.12,<3.13"

[tool.railway]
pythonVersion = "3.12"